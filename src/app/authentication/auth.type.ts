export type ICredential = {
    email: string,
    password?: string,
    otp?: string
}
export type IUser = {
    first_name: string,
    last_name: string,
    email: string,
    password?: string
}